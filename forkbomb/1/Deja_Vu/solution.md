открываем файл task.py, видим скрипт в котором необходимо ввести столько же символов, сколько в списке verify, то есть 7, затем над этими символами производят некоторые арифметические операции и затем просиходит сравнение с элементами того же спискка verify. Стоит обратить внимание на функцию ord, которая возвращает код символа кодировки Unicode. Для нахождения флага пишем свой скрипт где проделываем обратные арифметические операции над элементами списка verify 

формула для восстановления символов имеет вид (verify[i] + 337) // 13 - i

в итоге получаем строку "B3g3m0T"

ТАСКА РЕШЕНА
