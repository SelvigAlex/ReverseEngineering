Анализируя файл, делаем следующие выводы:
1. программа получает на вход два аргумента - название файла и строку
2. полученную строку ксорит с определенным числом и результат сохраняет в, указзаный при запуске программы, файл
3. неоднозначность с том, что ключ изменяющейся, что видно на фото, где каждый символ строки ксорится с ключом, а затем ключ по вот такому принципу обновляется key ^= str[i] 
<img width="1010" height="545" alt="image" src="https://github.com/user-attachments/assets/9b0f7326-be27-40fb-986a-307891d50bff" />
при подаче на вход файла encoded_flag, получаем на выходе строку "FLAG{c87ac46ff7d58780213623ab7a82e7f7}"
ТАСКА РЕШЕНА
