открываем исполняемый файл в IDA, попадаем в функцию main. Shift + F12 - открывается окно со строками вводим "Nope", так так именно эта строка, после того как мы запустили файл и введли строку "flag".
Ctrl + F, вводим "Nope" кликаем по единственной строке, попадаем в секцию rodata. Кликаем на на имя строки и нажимаем "X", видим единственную ссылку на эту строку, двойной клик по ней и попадаем в функцию sub_140003460.
Эта функция, где происходит основная проверка введеннйо строки, тут же мы видим строку "Well done!", которая скорее всего выводится при корректном флаге.
Анализируя ассемблерный код, видим что инициализируется массив на 30 элементов, также нетрудно обнаружить цикл проходящий по всем элементам массива, внутри которого существует проверка на длину массива, а также
на равенство элемента введенной строки умноженного на 3 и прибавленного к 5 элементу инициализированного массива
пишем "кейген" на с++ и ТАСКА РЕШЕНА
